/** Stable import surface for adapters. Kept separate so transports do not depend on builders. */
export type {
  PromptBundle,
  PromptContentPart,
  PromptKind,
  PromptMessage,
  PromptPriority,
  PromptSource,
  PromptSourceRef,
  PromptTrace,
  PromptVersion,
} from "./types";
